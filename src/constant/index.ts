export * from "./select";

export const BASE_URL = "https://pickriders-api-nest-dev.onrender.com"; // TODO: Replace this with url from env instead

export const API_URL = `${BASE_URL}/api/v1`;

export const STORAGE = {
  accessToken: "@pickriders/accessToken",
};
